# CLAUDE.md

このファイルはClaude Codeがこのプロジェクトで作業する際に自動的に読み込む規約ファイルです。

## プロジェクト概要

パルワールドの「拠点最適化シミュレーター」「戦闘最強ビルド/パーティ計算機」を作る。
経緯・データ仕様・確定済みの計算式は **`palworld_project_handoff.md`** に全部まとめてある。このファイルはこのリポジトリ内には無く、**Obsidian(MCP接続済み)側に保存されている**ので、作業開始前に必ずObsidian MCP経由でこのノートを検索・取得して読むこと(ファイル名 or ノートタイトルで検索: 「パルワールド 拠点/戦闘最適化プロジェクト 引き継ぎ仕様書」)。

## 絶対に守ること

1. **数値・計算式を記憶や推測で書かない。** `palworld_project_handoff.md` に確定値がある場合は必ずそれを使う。書いていない数値が必要になった場合は、Web検索で複数ソースを確認してから使う(1ソースだけで確定させない)。過去にGeminiが出典を偽装して誤った数値(濃縮の作業速度ボーナス、Lv→速度の指数関数など)を出してきた経緯があるので、特に「◯◯ごとに◯倍」のようなキリのいい数式は疑ってかかること。
2. **新しい数値情報を得たら、Obsidian内の `palworld_project_handoff.md` ノートを更新する。** 検証日・ソースURLも明記すること。ローカルリポジトリ内には複製を置かない(二重管理で情報が食い違う原因になるため)。
3. **巨大なJSONファイルの中身をターミナル出力やチャットに丸ごと表示しない。** 常に件数・サンプル1〜2件・要約だけ出力する。フルで見る必要があるときは `head` や `jq` で範囲を絞る。
4. **拠点最適化の計算はアルゴリズム(貪欲法+局所探索など)で行い、LLM推論に「どれが最適か」を判断させない。** 数値化できる問題はコードで解く。LLM(自分)の役割は「結果の解釈」「戦略の提案」「仕様のあいまいな部分の判断」に限定する。

## データファイルの場所

`data/` 以下(または実際に配置したパス)に以下を想定:
- `palworld_pals_clean.json`(287体、作業適正・タイプ・パートナースキル)
- `palworld_passives.json`(115種、日本語パッシブ)
- `palworld_combat_stats.json`(1123件、種族値・サイズ・移動速度)
- `palworld_work_speed_table.json`(役職ごとの実測作業速度テーブル)
- `game_data/`(ユーザー提供の内部データマイン一式: characters.json, skills.json, pals_learnset.json, breedingdata.json, pal_exp_table.json, work_suitability.json)

詳細なフィールド定義は `palworld_project_handoff.md` の「2. データファイル一覧」を参照。

## コーディング方針

- 実装済みの拠点プランナー(v1/v2)はReact不使用、素のHTML+CSS+JSの単一ファイル構成。同じ路線で問題なければ踏襲する
- **(2026-07-13更新) 画像アセット(パルアイコン等)はbase64埋め込みではなく`game_data/icons/`への相対パス参照を使う。** WebPは既に軽量でbase64化するとサイズが増える上、今後アイテム/技アイコンなど画像を使う機能が増える見込みのため、HTMLファイルへの丸ごと埋め込みは避ける。このプロジェクトは`palworld/`フォルダ一式(HTML+game_data等)をセットで扱う前提とし、HTML単体でのポータビリティは重視しない
- デザインは「パルワールドっぽい遊び心のあるかっこいいデザイン」を目指す。AIが作った感の強い汎用UI(白背景+紫グラデーションのような既視感のあるやつ)は避ける。木材・革・力強いフォントなどクラフト/サバイバルゲームらしい質感を意識する
- デザインの本格的な作り込みは、戦闘データ(属性相性表、技威力とパルの紐付け)が揃ってから一括でやる方針(手戻り防止)
- 言語は日本語で応答する

## 現在地・次のタスク

`palworld_project_handoff.md` の「6. 次にやるべきこと」を参照。優先順位は以下の通り:
1. 属性相性の倍率表を確定
2. `skills.json`の`passives`(1905件、英語)を日本語版とクロスチェックして統合
3. アクティブスキルとパルの技習得ルートの紐付け
4. ダメージ計算式の確定
5. 「1体最強ビルド」計算ロジック
6. 「6体最強パーティ」計算ロジック
7. 拠点v2+戦闘計算の統合とデザイン作り込み
