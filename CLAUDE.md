# CLAUDE.md

このファイルはClaude Codeがこのプロジェクトで作業する際に自動的に読み込む規約ファイルです。

## プロジェクト概要

パルワールドの「拠点最適化シミュレーター」「戦闘最強ビルド/パーティ計算機」を作る。
経緯・データ仕様・確定済みの計算式は **`palworld_project_handoff.md`** に全部まとめてある。このファイルはこのリポジトリ内には無く、**Obsidian(MCP接続済み)側に保存されている**(ノートタイトル: 「パルワールド 拠点/戦闘最適化プロジェクト 引き継ぎ仕様書」)。

**読み方(2026-08-09改訂)**: このノートは約8万文字(3.7万トークン)ある。**冒頭で全文を取得しないこと。** セッションを開くたびに全文を読むと、その3.7万トークンが以降の全ターンで再送され続ける。

1. まず `obsidian_search_notes` で**必要な節だけ**引く(例: 「作業速度」「配合」「属性相性」)
2. それで足りないときだけ `obsidian_get_note` で全文を取る
3. 数値を1つ確認したいだけなら、検索のヒット文脈で足りることがほとんど

## 絶対に守ること

1. **数値・計算式を記憶や推測で書かない。** `palworld_project_handoff.md` に確定値がある場合は必ずそれを使う。書いていない数値が必要になった場合は、Web検索で複数ソースを確認してから使う(1ソースだけで確定させない)。過去にGeminiが出典を偽装して誤った数値(濃縮の作業速度ボーナス、Lv→速度の指数関数など)を出してきた経緯があるので、特に「◯◯ごとに◯倍」のようなキリのいい数式は疑ってかかること。
2. **新しい数値情報を得たら、Obsidian内の `palworld_project_handoff.md` ノートを更新する。** 検証日・ソースURLも明記すること。ローカルリポジトリ内には複製を置かない(二重管理で情報が食い違う原因になるため)。
3. **巨大なJSONファイルの中身をターミナル出力やチャットに丸ごと表示しない。** 常に件数・サンプル1〜2件・要約だけ出力する。フルで見る必要があるときは `head` や `jq` で範囲を絞る。
4. **拠点最適化の計算はアルゴリズム(貪欲法+局所探索など)で行い、LLM推論に「どれが最適か」を判断させない。** 数値化できる問題はコードで解く。LLM(自分)の役割は「結果の解釈」「戦略の提案」「仕様のあいまいな部分の判断」に限定する。

## ポケットペアの二次創作ガイドライン(このサイトの大前提)

**禁止事項**: ①有償無償に関わらず**事業性の高い営利目的での利用** ②個人・団体・企業への攻撃や誹謗中傷等を目的とする、あるいはそう受け取られる表現 ③政治・宗教・特定の信条の宣伝 ④公序良俗に反する表現 ⑤第三者の権利を侵害する恐れのある表現 ⑥**当社の公式製品と誤認される可能性のある表現** ⑦その他、想定外に副次的に発生する不適切と判断されるもの

**その他**: 禁止事項に触れなければ事前連絡なく自由に二次創作可能 / ガイドラインは保証ではなく責任は運営者側 / 違反と判断されれば停止を求められる場合がある / 内容は予告なく変更されうる / **個別の問い合わせには基本的に返答しない**

作業上の意味:

1. **収益化(広告・投げ銭・有料機能)を提案・実装する前に必ず颯太さんに確認する。** ①に触れる可能性があり、しかも問い合わせても返答が来ないため事前にお墨付きを取る道が無い。GitHub Pagesの商用禁止規約よりこちらのほうが重い制約
2. ⑥があるため、**新しいページを追加したら必ずフッターに「非公式ファンメイド」と `© Pocketpair, Inc.` の表記を入れる。** 既存ページの文面をそのまま流用すること
3. 「停止を求められうる」前提を崩さない。利用者から金銭を預かる仕組みや、止まると第三者に迷惑がかかる仕組みは作らない
4. ⑤の観点で、他サイトのデータを使うときは出典を正確に書く。**やり取りが無いのに「提供による」のような協力関係を示す表現を書かない**

## 他サイトのデータを使ってよいかの基準

線引きは「どのサイトか」ではなく「**生のゲームデータか、そのサイトが編集した結果か**」。

| | 使ってよい | 使わない |
|---|---|---|
| 中身 | ゲームが持っている数値そのもの | そのサイトが編集・分類・整理した結果 |
| 例 | パル像のworld座標、種族値、技威力、施設の仕様 | 出現をまとめた円の描き方、独自ランク、独自分類、解説文 |

**判断は主観でやらない。別ソースと突き合わせて数値が一致するかを実際に確かめる。** 一致すれば「そのサイトが作ったものではなく、ゲームが持っている数値」の証拠になる。

実例が2つある。どちらも同じ基準で逆の結論が出ているので、迷ったら読むこと:

- **採用した例**: `scripts/build_statue_data.py` の冒頭コメント。palworld-lab.com のパル像407件は、raw world座標が paldb.cc 由来の座標とほぼ一致することを確認したうえで採用している(座標変換式は自前の実測フィット式と Nifrendil/pal-atlas のもので、向こうのものは使っていない)
- **見送った例**: 出現マップ。palworld-lab.com のものは「近い湧きをまとめた円」という**編集結果**だったため、複製に当たると判断して不採用にし、代わりに Awy64/palworld-atlas-data(ゲーム本体の生成テーブルを読むオープンソース)を採用した

いずれの場合も出典は明記する。

## 配合データはどれが本番か(2026-08-10 追記)

似た名前のファイルが3つあり、**中身の完全さが違う。** 見分けがつかず一度間違えた。

| ファイル | forwardPairs | 位置づけ |
|---|---|---|
| `game_data/breedingdata.json` | — | 生データ |
| `game_data/breedingdata_v2.json` | **28,330件(不完全)** | 中間生成物。**参照しないこと** |
| `game_data/breeding_data.js` | **44,849件(全網羅)** | **本番。サイトはこれを読む** |
| `game_data/breeding_pals_data.js` | (ペア表なし) | 軽量版。パルの基本情報+combi_rankのみ |

**配合の子を調べるときは必ず `breeding_data.js` を見る。** `breedingdata_v2.json` は
組み合わせが4割欠けており、そこを見て「配合表に穴がある」「一般則で補う必要がある」と
誤った結論を出した(実際は全網羅されていて、推定式は不要)。

`breeding_pals_data.js` の combi_rank は `breeding_data.js` と完全一致することを確認済み
(299体、食い違い0)。ランクだけ要るとき(突然変異の計算など)は軽量版で足りる。

## データファイルの場所

`data/` 以下(または実際に配置したパス)に以下を想定:
- `palworld_pals_clean.json`(287体、作業適正・タイプ・パートナースキル)
- `palworld_passives.json`(115種、日本語パッシブ)
- `palworld_combat_stats.json`(1123件、種族値・サイズ・移動速度)
- `palworld_work_speed_table.json`(役職ごとの実測作業速度テーブル)
- `game_data/`(ユーザー提供の内部データマイン一式: characters.json, skills.json, pals_learnset.json, breedingdata.json, pal_exp_table.json, work_suitability.json)

詳細なフィールド定義は `palworld_project_handoff.md` の「2. データファイル一覧」を参照。

## コーディング方針

- 実装済みの拠点プランナー(v1/v2)はReact不使用、素のHTML+CSS+JSの単一ファイル構成。同じ路線で問題なければ踏襲する
- **(2026-07-13更新) 画像アセット(パルアイコン等)はbase64埋め込みではなく`game_data/icons/`への相対パス参照を使う。** WebPは既に軽量でbase64化するとサイズが増える上、今後アイテム/技アイコンなど画像を使う機能が増える見込みのため、HTMLファイルへの丸ごと埋め込みは避ける。このプロジェクトは`palworld/`フォルダ一式(HTML+game_data等)をセットで扱う前提とし、HTML単体でのポータビリティは重視しない
- デザインは「パルワールドっぽい遊び心のあるかっこいいデザイン」を目指す。AIが作った感の強い汎用UI(白背景+紫グラデーションのような既視感のあるやつ)は避ける。木材・革・力強いフォントなどクラフト/サバイバルゲームらしい質感を意識する
- デザインの本格的な作り込みは、戦闘データ(属性相性表、技威力とパルの紐付け)が揃ってから一括でやる方針(手戻り防止)
- 言語は日本語で応答する

## 現在地・次のタスク

`palworld_project_handoff.md` の「6. 次にやるべきこと」を参照。優先順位は以下の通り:
1. 属性相性の倍率表を確定
2. `skills.json`の`passives`(1905件、英語)を日本語版とクロスチェックして統合
3. アクティブスキルとパルの技習得ルートの紐付け
4. ダメージ計算式の確定
5. 「1体最強ビルド」計算ロジック
6. 「6体最強パーティ」計算ロジック
7. 拠点v2+戦闘計算の統合とデザイン作り込み
